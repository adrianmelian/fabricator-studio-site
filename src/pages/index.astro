---
// The homepage. Rebuilt 2026-07-22 (website overhaul, two-door architecture).
//
// What changed and why, so nobody re-litigates it from the diff:
//
// The page used to be a product page wearing a studio's name. Its first four screens offered
// one action, nothing said a person was for hire until a visitor found the Services tab, and
// it ended on the bio with no ask at all. It also argued twice: six adjective cards sat
// directly under four blocks that DEMONSTRATED the same claims, and a tutorials grid spent a
// whole section saying that a nav item exists.
//
// The governing rule of the pass was a FIXED SECTION BUDGET: every addition names the element
// it replaces, and the page may end shorter but never longer. It ended shorter.
//
//   key features (6 adjective cards)  ->  BUILT WITH IT (the proof section)
//   tutorials grid                    ->  WORK WITH ME (the closing ask, the page had none)
//   hero sub 2 ("Generative AI era")  ->  the studio line, and it now leads the hero
//   the 4th repeat of the free line   ->  the shipped record, credited to the person
//   the four tool cards + popups      ->  relocated INTACT to /tools/, which can grow
//   the licensing tiers               ->  relocated to /tools/, next to what they price
//
// Deliberately untouched: the hero video, the wordmark, the download gate and its
// MailerLite/R2 path, the welcome video's position, the WHY quote (Adrian's verbatim story),
// and Mindmeld 2.0 styling everywhere.
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import BracketButton from '../components/BracketButton.astro';
import NavBar from '../components/NavBar.astro';
import Footer from '../components/Footer.astro';
import WelcomeVideo from '../components/WelcomeVideo.astro';
import SectionHead from '../components/SectionHead.astro';
import Pill from '../components/Pill.astro';
import YouTubeFacade from '../components/YouTubeFacade.astro';
import DownloadBand from '../components/DownloadBand.astro';
import FabricatorHero from '../components/FabricatorHero.astro';

import trailerPoster from '../assets/armature/trailer-poster.jpg';
import heroWordmark from '../assets/hero/wordmark-banner.png';
// One flag governs every control-rig claim on the site (R25, R27). See src/data/armature.ts.
import { ARMATURE, MAYA_CONTROL_RIG } from '../data/armature';

// BUILT WITH IT. Three entries, not four: Archangel is held back until Adrian clears the
// game's art direction for public view, and a filler fourth would weaken the three that
// carry real weight. Every claim here is verifiable today, which is the entire point of the
// section: we out-prove the field, we were just never showing it.
//
// `pill` is plasma for shipped/live states (DNA principle 2: plasma = success/correct).
// Ember stays reserved for IN DEVELOPMENT, which nothing on this page currently carries.
//
// Media: a poster still that carries the card on its own, and a six-second silent loop that
// plays on hover or keyboard focus. NOT autoplay. The hero already autoplays a video, three
// more loops firing on load would cost real weight and battery for motion most visitors
// never look at, and the still is what has to read at thumbnail size regardless. On touch,
// under reduced-motion, and with JS off, the poster simply stands on its own.
//
// Both clips are 4:3, which is why the media box is no longer 16:10: both subjects are
// standing figures, and a wide box spent most of its area on floor and empty background.
//
// `shot` names the asset an entry is still waiting on (MrMiata:
// workspace/2026-07-22_website-overhaul/SHOT-LIST.md). An entry with no media renders a
// visibly unfinished plate rather than silently collapsing.
const PROOF = [
  {
    // Replaced the Reggie card 2026-07-29 (Adrian). Three characters carry more weight than
    // one, and these are the ones the pipeline is being built on now. Reggie's clip and poster
    // are still in public/proof/ if this needs reverting.
    // The clip shows three characters. Saying so in the title, again in the claim, is telling a
    // reader what they can already see. The copy carries the part the picture cannot: that all
    // of them took the same route to get there.
    name: 'Armature characters',
    pill: 'IN UNREAL',
    claim: 'Rigged, skinned, and animating in engine, all through the same route.',
    detail:
      'Joints placed by hand, skin weights solved on the machine, and one USD file carrying the mesh, skeleton, orientations and weights into Unreal, where the Fabricator Control Rig is built procedurally from that same export data.',
    video: '/proof/proof-unreal-montage.mp4',
    poster: '/proof/proof-unreal-montage.jpg',
    alt: 'A samurai, a ninja and a lumberjack character animating in the Unreal viewport.',
  },
  {
    // Replaced the QS_BipedRig card 2026-07-29 (Adrian). Its clip and poster stay in
    // public/proof/, so restoring it is a data change and nothing else.
    //
    // The clip shows Maya and Unreal, and it says so itself with both logos in frame. What it
    // cannot show is that neither rig was authored: both are generated from the same export.
    name: 'The Fabricator Control Rig',
    pill: 'MAYA + UNREAL',
    claim: 'Built procedurally in both, from one export.',
    // R27 item 3: this card had the same shipped-versus-demonstrated gap /armature's VERIFIED
    // panel got corrected for under R25. Reading the SAME flag rather than hardcoding a
    // sentence means the homepage and /armature cannot describe the toolset differently, which
    // is the actual risk when one claim lives in two places.
    detail:
      'The USD export carries control rig build data alongside the mesh, skeleton and weights. Both rigs are generated from it, so the controls, their shapes and their hierarchy match without anyone rebuilding them on the far side. ' +
      (MAYA_CONTROL_RIG
        ? 'The Maya builder ships free in the toolset today; the Unreal builder is what we are working on next.'
        : 'Both builders are what we are working on next.'),
    video: '/proof/proof-controlrig.mp4',
    poster: '/proof/proof-controlrig.jpg',
    // Poster is the crossfade frame at t=3.5, where both logos and both rigs are on screen at
    // once. It makes the point before anyone presses play.
    alt: 'The same character and control rig shown in Maya and then in Unreal.',
  },
];
---
<BaseLayout
  title="FabricatorStudio | The Artist-Friendly Game Dev Pipeline"
  description="A one-person technical art studio. The free Maya to Unreal character pipeline, and the 14-year AAA technical artist who built it, available for hire."
>
  <NavBar />

  <section id="hero" class="hero-v2">
    <video class="hero-media" autoplay muted loop playsinline
      poster="/demo/fs_hero_poster.jpg"
      src="/demo/fs_hero.mp4"
      aria-hidden="true">
    </video>
    <img class="hero-media hero-poster" src="/demo/fs_hero_poster.jpg" alt="" aria-hidden="true" />
    <div class="hero-scrim" aria-hidden="true"></div>
    <div class="hero-copy">
      <h1 class="hero-wordmark">
        <Image src={heroWordmark} alt="FabricatorStudio" widths={[1180, 2205]} sizes="(max-width: 820px) 92vw, 1180px" loading="eager" />
      </h1>
      <p class="hero-tagline">HUMAN+AI STUDIO TOOLSET</p>
      <div class="hero-divider" aria-hidden="true">
        <span class="hero-divider__bar"></span>
        <span class="hero-divider__gem"></span>
        <span class="hero-divider__bar"></span>
      </div>
      {/* Adrian's own rewrite, 2026-08-01 evening, shipped as written. The philosophy leads
          now: Artist Driven AI tools, the dev-to-artist floor-and-ceiling, and the refinement
          contrast. He knowingly kept the "These are not AI tools" shape against the anti-slop
          Tier A rule; owner's override, recorded here so nobody "fixes" it back. The
          available-for-hire line moved out of the hero on the same pass; hiring converts in
          the closing band, after the proof. */}
      <p class="hero-sub hero-sub--studio"><b>FabricatorStudio is a one-person Technical Art studio making Artist Driven AI tools.</b> <b class="hero-sub__hl">Free for small studios.</b> These are not AI tools, they are <b class="hero-sub__ember">Artist tools with AI assistance</b>.</p>
      <p class="hero-sub">A dev generates a <b class="hero-sub__hl">working rig in minutes</b>; a character artist pushes it to <b class="hero-sub__ember">production level</b>. Most AI tooling is built without refinement in mind. <b>FabricatorStudio tools are built so you can drive your asset to production level standards.</b></p>
      {/* Two doors, unequal on purpose. Most arrivals today come for the toolset, so the free
          download keeps the visual weight it has always had and the paid door sits beside it
          rather than competing with it. Hiring is NOT in the hero: it is a different audience
          and it converts better after the proof, in the closing band. */}
      <div class="hero-ctas">
        <BracketButton kind="hard" installer label="Get the free toolset" event="hero-download" />
        <BracketButton kind="soft" href="/store/" label="Advanced Modules →" event="hero-store" />
      </div>
    </div>
  </section>

  {/* ARMATURE, directly under the hero (Adrian, 2026-07-29). It was mid-page, after the free
      Maya toolset, on the argument that "the same work without a Maya seat" only reads once a
      visitor knows what the Maya toolset is. That argument loses to the simpler one: Armature
      is the newest thing the studio has, and burying the newest thing below four sections is
      how announcements get missed.

      The media is the 86-second voiceover trailer on YouTube, loaded through a facade so
      nothing reaches Google until a visitor actually clicks play. Copy is lifted verbatim from
      the /armature/ hero, so the card and the page it opens cannot drift. */}
  <section id="armature">
    <SectionHead eyebrow="FABRICATORSTUDIO" title="ARMATURE" />
    <div class="arm-grid">
      <YouTubeFacade
        class="arm-media"
        id="PhHXmcpcGKk"
        title="Armature | Standalone Rigging & Skinning App | Unreal"
        poster={trailerPoster}
        posterAlt="A samurai character rigged in Armature, from the Armature trailer"
        widths={[720, 1440]}
        sizes="(min-width: 900px) 56vw, 92vw"
      />
      <div class="arm-body">
        <Pill text={`Out now · ${ARMATURE.version}`} tone="plasma" />
        <h3 class="arm-head">High End Character Rigging</h3>
        <p class="arm-lead">has always lived inside expensive applications. Armature is for the indie dev who cannot justify one, and the veteran rigger who wants to move faster.</p>
        {/* The clarifying line for toolset owners carries the thesis (SPEC-SITE-LAUNCH item 1),
            worded for a reader who already knows the Maya toolset. */}
        <p class="arm-note">Armature is a new standalone application, no Maya seat needed: its AI half gets a character rigged in minutes, and its hand tools refine the result to production level.</p>
        <div class="arm-ctas">
          <BracketButton kind="hard" href="/armature/" label="See Armature" event="home-armature" />
        </div>
      </div>
    </div>
  </section>

  {/* WAS: six key-feature cards. They claimed "Easy to use", "Studio-grade tools", and
      "Non-destructive workflow" immediately after four blocks that demonstrated exactly
      those things with real UI. Claims placed after proof are worse than useless; they
      weaken the proof. This section shows what the pipeline has MADE instead. */}
  <section id="proof">
    <SectionHead eyebrow="FABRICATORSTUDIO" title="BUILT WITH IT" />
    <div class="pf-grid">
      {PROOF.map((p) => (
        <article class="pf-card">
          <div class="pf-media">
            {p.video ? (
              <>
                <video class="pf-vid" src={p.video} poster={p.poster} muted loop playsinline preload="none" aria-label={p.alt}></video>
                <span class="pf-play" aria-hidden="true"></span>
              </>
            ) : (
              <div class="pf-pending" role="img" aria-label={`${p.name}: media pending`}>
                <span class="pf-shot">ASSET PENDING</span>
              </div>
            )}
          </div>
          <div class="pf-body">
            <div class="pf-top">
              <h3 class="pf-name">{p.name}</h3>
              <span class="pf-pill">{p.pill}</span>
            </div>
            <p class="pf-claim">{p.claim}</p>
            <details class="pf-more">
              <summary class="pf-more__q">How</summary>
              <p class="pf-more__a">{p.detail}</p>
            </details>
          </div>
        </article>
      ))}

      {/* The third entry's evidence is textual rather than photographic, so it is set as
          type instead of faked as a screenshot. Every line below is checkable by clicking
          it, which is a stronger argument than a picture of a repo page would have been. */}
      <article class="pf-card">
        <div class="pf-media pf-media--type">
          <span class="pf-v">v1</span>
          <span class="pf-v__lic">BUSL-1.1, source available</span>
          <div class="pf-v__links">
            <a href="/docs/">Docs</a>
            <a href="/changelog/">Changelog</a>
            <a href="/known-issues/">Known issues</a>
            <a href="https://github.com/adrianmelian/FabricatorStudio" target="_blank" rel="noopener">Source</a>
          </div>
        </div>
        <div class="pf-body">
          <div class="pf-top">
            <h3 class="pf-name">Maya Toolset v1</h3>
            <span class="pf-pill">SHIPPED</span>
          </div>
          <p class="pf-claim">Public source, full docs, a public issue tracker, and outside users.</p>
          <details class="pf-more">
            <summary class="pf-more__q">How</summary>
            <p class="pf-more__a">Shipped 2026-07-20 under BUSL-1.1, with documentation, a public issue tracker, a changelog, and a known-issues page. Every version becomes fully open source four years after it ships, automatically.</p>
          </details>
        </div>
      </article>
    </div>
  </section>

  {/* WAS: four full-bleed tool cards. They were the strongest thing on the site and they are
      not lost: they moved to /tools/ intact, popups and all, where the toolset can grow
      without making this page longer every time a tool ships. What stays here is one block
      that names the box and gets out of the way, carrying the Fabricator graphic so the
      homepage keeps a real visual anchor for the product. */}
  <section id="toolset">
    <SectionHead eyebrow="FABRICATORSTUDIO" title="THE TOOLSET" />
    <div class="tk-grid">
      <div class="tk-ui"><FabricatorHero /></div>
      <div class="tk-body">
        <p class="tk-lead">Modular rigging, automatic skinning, a cross-rig pose and animation library, and FBX exporters with an engine-ready contract.</p>
        <p class="tk-line">Pure Python, source available, nothing to compile, nothing hidden.</p>
        <p class="tk-lic">Free for studios under $2 million a year. <a href="/licensing/">See licensing</a>.</p>
        <div class="tk-ctas">
          <BracketButton kind="hard" href="/tools/" label="See the toolset" event="home-toolset" />
        </div>
      </div>
    </div>
  </section>

  <DownloadBand />

  {/* WAS: the tutorials grid. It spent a full section telling a visitor that a page in the
      nav exists, using YouTube's thumbnails, which made it the least on-brand block on the
      page. Tutorials keep their nav item, their page, and a footer link. In its place, the
      thing the homepage never had: an ask. */}
  <section id="hire">
    <div class="hb-card">
      <h2 class="hb-head">Available for engagements.</h2>
      <p class="hb-line">Rig and export setup, custom Maya tools, fractional TA and retainers. Delivered by the technical artist who built Fabricator, documented, and yours to keep.</p>
      <div class="hb-ctas">
        <BracketButton kind="hard" href="/services/" label="See services" event="home-services" />
        <a class="hb-mail" href="mailto:adrian@fabricator.studio">adrian@fabricator.studio</a>
      </div>
    </div>
  </section>

  {/* ABOUT ME is now the spoken introduction rather than a wall of prose (Adrian, 2026-07-29).
      The welcome video used to sit directly under the hero; Armature took that slot, and this
      is where it landed. A section titled ABOUT ME whose content is Adrian talking to camera
      is a better answer to "who is this" than three paragraphs and a pull quote were.

      REMOVED with the prose, and worth knowing rather than discovering later: the bio
      paragraphs, the WHY pull quote, and the "14 YEARS / SHIPPED" title list. The WHY story is
      still live on /licensing, and the full credential is still on /armature under "Who built
      it", so neither is lost from the site. It IS gone from the homepage. Restoring the
      shipped-titles strip alone is a small edit if that trade turns out to be wrong. */}
  <section id="about">
    <SectionHead eyebrow="FABRICATORSTUDIO" title="ABOUT ME" />
    <WelcomeVideo />
  </section>

  <Footer />

  {/* Proof cards: poster by default, loop on hover or keyboard focus.
      Bails entirely on touch (no hover pointer) and under reduced-motion, where the poster
      is the whole experience and preload="none" means the clip is never even fetched. */}
  <script is:inline>
    (function () {
      var mm = window.matchMedia;
      if (!mm) return;
      if (mm('(prefers-reduced-motion: reduce)').matches) return;
      if (!mm('(hover: hover)').matches) return;
      document.querySelectorAll('.pf-card').forEach(function (card) {
        var v = card.querySelector('.pf-vid');
        if (!v) return;
        function play() {
          card.classList.add('is-playing');
          var p = v.play();
          // Autoplay policy can still reject a programmatic play(); swallow it rather than
          // throwing an unhandled rejection into the console on every hover.
          if (p && p.catch) p.catch(function () { card.classList.remove('is-playing'); });
        }
        function stop() {
          v.pause();
          card.classList.remove('is-playing');
        }
        card.addEventListener('pointerenter', play);
        card.addEventListener('pointerleave', stop);
        card.addEventListener('focusin', play);
        card.addEventListener('focusout', stop);
      });
    })();
  </script>

  <script is:inline>
    (function () {
      if (window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
      // Modern browsers (incl. mobile) get smooth compositor parallax from CSS (base.css).
      // This JS only runs as a fallback where scroll-driven animations are unsupported.
      if (window.CSS && CSS.supports && CSS.supports('animation-timeline: view()')) return;
      var pairs = [].slice.call(document.querySelectorAll('[data-parallax]'))
        .map(function (img) { return { img: img, win: img.closest('[data-parallax-window]') }; })
        .filter(function (pr) { return pr.win; });
      if (!pairs.length) return;
      var ticking = false;
      function update() {
        ticking = false;
        var vh = window.innerHeight || document.documentElement.clientHeight;
        for (var i = 0; i < pairs.length; i++) {
          var img = pairs[i].img, win = pairs[i].win;
          var overflow = img.offsetHeight - win.offsetHeight;
          if (overflow <= 0) { img.style.transform = ''; continue; }
          var rect = win.getBoundingClientRect();
          var p = (vh - rect.top) / (vh + rect.height);
          p = p < 0 ? 0 : p > 1 ? 1 : p;
          img.style.transform = 'translateY(' + (-overflow * (1 - p)).toFixed(1) + 'px)';
        }
      }
      function onScroll() { if (!ticking) { ticking = true; requestAnimationFrame(update); } }
      window.addEventListener('scroll', onScroll, { passive: true });
      window.addEventListener('resize', onScroll);
      update();
    })();
  </script>
</BaseLayout>

<style>
  .hero-v2 {
    position: relative;
    overflow: hidden;
    width: 100vw;
    max-width: 100vw;
    margin: 0 0 0 calc(50% - 50vw);
    padding: 0;
    /* svh, not vh: on mobile vh measures the viewport with the toolbars RETRACTED, so a
       100vh hero is taller than what you can see and the next section peeks on load.
       min-height, not height: .hero-v2 clips its overflow and a fixed height would
       guillotine the CTAs on short screens. */
    min-height: calc(100vh - var(--header-h));
    min-height: calc(100svh - var(--header-h));
    display: flex;
    align-items: center;
    justify-content: flex-end;
    flex-direction: column;
  }
  .hero-media {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }
  .hero-v2 video.hero-media { display: block; }
  .hero-v2 img.hero-poster { display: none; }
  @media (prefers-reduced-motion: reduce) {
    .hero-v2 video.hero-media { display: none; }
    .hero-v2 img.hero-poster { display: block; }
  }
  .hero-scrim {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(to top, rgba(11, 14, 16, 0.92) 0%, rgba(11, 14, 16, 0.55) 40%, rgba(11, 14, 16, 0) 65%);
  }
  /* A full-bleed BAND, not a floating plate. The video is the worst offender for legibility:
     it MOVES, so a scrim that reads fine on one frame washes the copy out on the next. A
     plate makes legibility independent of whatever frame is playing behind it. */
  .hero-copy {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    text-align: center;
    align-self: stretch;
    min-height: calc(0.6 * (100vh - var(--header-h)));
    min-height: calc(0.6 * (100svh - var(--header-h)));
    padding: 1.75rem 2rem;
    background: rgba(11, 14, 16, 0.31);
    /* Unprefixed ONLY: hand-writing the -webkit- twin makes lightningcss drop the standard
       property and Firefox loses the blur silently. */
    backdrop-filter: blur(6px) saturate(115%);
    border: 0;
    border-top: 1px solid rgba(232, 224, 208, 0.1);
    border-bottom: 1px solid rgba(232, 224, 208, 0.1);
    box-shadow: var(--shadow-lg);
    text-shadow: 0 1px 2px rgba(11, 14, 16, 0.55);
  }
  /* Tests the STANDARD property only. ORing in the -webkit- alias would make this false on
     older Safari (which supports the alias) while the build emits no alias declaration, so
     Safari would land on a see-through plate with no blur and no fallback. */
  @supports not (backdrop-filter: blur(1px)) {
    .hero-copy { background: rgba(11, 14, 16, 0.55); }
  }
  .hero-wordmark { margin: 0; width: min(73.75rem, 100%); }
  .hero-wordmark :global(img) {
    display: block;
    width: 100%;
    height: auto;
    filter: drop-shadow(0 18px 40px rgba(11, 14, 16, 0.55));
  }
  .hero-tagline { margin: 0; font-family: var(--font-mono); font-weight: 700; font-size: clamp(1rem, 2vw, 1.5rem); letter-spacing: 0.28em; color: var(--ember); }
  .hero-divider { display: flex; align-items: center; gap: 0.75rem; }
  .hero-divider__bar { width: 3rem; height: 3px; background: var(--plasma); border-radius: var(--radius-pill); }
  .hero-divider__gem { width: 0.55rem; height: 0.55rem; background: var(--plasma); transform: rotate(45deg); }
  .hero-sub { font-family: var(--font-body); font-size: 1.0625rem; line-height: 1.7; color: var(--bone); max-width: 52rem; }
  /* The posture line reads a step up from the description under it: mono, slightly larger,
     so the first thing a visitor parses is what this place IS. */
  .hero-sub--studio { font-family: var(--font-mono); font-size: clamp(1.0625rem, 1.6vw, 1.1875rem); }
  .hero-sub b { color: var(--bone); }
  .hero-sub .hero-sub__hl { color: var(--plasma); }
  .hero-sub .hero-sub__ember { color: var(--ember); }
  .hero-ctas { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; margin-top: 0.75rem; }

  /* BUILT WITH IT. Reuses the card furniture the key-features grid used, so this cost
     styling work rather than design work, and the section it replaces is not missed. */
  #proof { text-align: left; }
  .pf-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 1.125rem; }
  @media (max-width: 900px) { .pf-grid { grid-template-columns: 1fr; } }
  .pf-card {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    background: linear-gradient(180deg, var(--iron), var(--carbon));
    border: 1px solid rgba(124, 255, 178, 0.14);
    border-radius: 16px;
    overflow: hidden;
    box-shadow: var(--shadow-md);
  }
  /* 4:3, not 16:10. Both proof subjects are standing figures, and a wide box spent most of
     its area on floor and empty background. */
  .pf-media {
    position: relative;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    border-bottom: 1px solid rgba(124, 255, 178, 0.18);
    background: radial-gradient(120% 120% at 50% 38%, var(--iron-2), var(--carbon));
  }
  .pf-vid { display: block; width: 100%; height: 100%; object-fit: cover; }
  /* The affordance, and the only thing that says the still is also a clip. It fades out
     once playback starts, so a playing card is not wearing a play button. */
  .pf-play {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 2.75rem;
    height: 2.75rem;
    margin: -1.375rem 0 0 -1.375rem;
    border-radius: 50%;
    background: rgba(11, 14, 16, 0.55);
    border: 1px solid rgba(124, 255, 178, 0.5);
    pointer-events: none;
    transition: opacity var(--t-base) var(--ease-out);
  }
  .pf-play::after {
    content: '';
    position: absolute;
    left: 55%;
    top: 50%;
    transform: translate(-50%, -50%);
    border-top: 0.45rem solid transparent;
    border-bottom: 0.45rem solid transparent;
    border-left: 0.75rem solid var(--plasma);
  }
  .pf-card.is-playing .pf-play { opacity: 0; }
  /* No hover pointer means no hover affordance to advertise. */
  @media (hover: none) {
    .pf-play { display: none; }
  }

  .pf-pending {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1.25rem;
    text-align: center;
  }
  .pf-shot {
    font-family: var(--font-mono);
    font-weight: 800;
    font-size: 0.6875rem;
    letter-spacing: 0.22em;
    color: var(--ember);
    border: 1px solid rgba(255, 122, 61, 0.45);
    background: rgba(255, 122, 61, 0.13);
    border-radius: 8px;
    padding: 0.3rem 0.7rem;
  }
  /* The v1 entry: its evidence is textual, so it is set as type rather than faked. */
  .pf-media--type {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 1.25rem;
    text-align: center;
  }
  .pf-v { font-family: var(--font-display); font-weight: 800; font-size: clamp(2.5rem, 6vw, 3.5rem); line-height: 1; color: var(--plasma); }
  .pf-v__lic { font-family: var(--font-mono); font-size: 0.75rem; letter-spacing: 0.14em; color: var(--bone-dim); }
  .pf-v__links { display: flex; flex-wrap: wrap; justify-content: center; gap: 0.5rem 1rem; margin-top: 0.5rem; }
  .pf-v__links a { font-family: var(--font-mono); font-size: 0.8125rem; color: var(--bone); text-decoration: none; border-bottom: 1px solid rgba(124, 255, 178, 0.4); }
  .pf-v__links a:hover { color: var(--plasma); }

  .pf-body { display: flex; flex-direction: column; gap: 0.75rem; padding: 1.5rem 1.5rem 1.625rem; }
  .pf-top { display: flex; align-items: center; justify-content: space-between; gap: 0.75rem; flex-wrap: wrap; }
  .pf-name { margin: 0; font-family: var(--font-display); font-weight: 800; font-size: 1.1875rem; line-height: 1.1; color: var(--bone); }
  /* plasma = success / correct (principle 2). Ember stays reserved for IN DEVELOPMENT. */
  .pf-pill {
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 0.6875rem;
    letter-spacing: 0.14em;
    color: var(--plasma);
    background: rgba(124, 255, 178, 0.13);
    border: 1px solid rgba(124, 255, 178, 0.45);
    border-radius: 8px;
    padding: 0.25rem 0.6rem;
    white-space: nowrap;
  }
  .pf-claim { margin: 0; font-size: 0.9375rem; line-height: 1.6; color: var(--bone); text-wrap: pretty; }
  /* Scan on the surface, deep on demand, both work: the same promise the tool cards make,
     without a second modal system to maintain for one paragraph of detail. */
  .pf-more { margin-top: auto; }
  .pf-more__q {
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 0.8125rem;
    letter-spacing: 0.06em;
    color: var(--plasma);
    cursor: pointer;
    list-style: none;
  }
  .pf-more__q::-webkit-details-marker { display: none; }
  .pf-more__q::after { content: ' +'; }
  .pf-more[open] .pf-more__q::after { content: ' \2212'; }
  .pf-more__a { margin: 0.75rem 0 0; font-size: 0.875rem; line-height: 1.6; color: var(--bone-dim); text-wrap: pretty; }

  /* THE TOOLSET: one compact block where four full-bleed cards used to be. */
  #toolset { text-align: left; }
  .tk-grid { display: grid; grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr); gap: 2.5rem; align-items: center; }
  @media (max-width: 900px) { .tk-grid { grid-template-columns: 1fr; gap: 1.75rem; } }
  .tk-ui { border-radius: 16px; overflow: hidden; border: 1px solid var(--iron-3); box-shadow: var(--shadow-lg); }
  .tk-body { display: flex; flex-direction: column; gap: 1rem; }
  .tk-lead { margin: 0; font-size: 1.0625rem; line-height: 1.65; color: var(--bone); text-wrap: pretty; }
  .tk-line { margin: 0; font-size: 0.9375rem; line-height: 1.6; color: var(--bone-dim); }
  .tk-lic { margin: 0; font-family: var(--font-mono); font-size: 0.875rem; color: var(--bone-faint); }
  .tk-ctas { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0.25rem; }

  /* ARMATURE: the 86-second trailer carries this block, so the media leads and takes the wider
     column. It is 16:9 now (the block used to be built portrait-first around the vertical
     teaser cut). The frame itself is the YouTubeFacade component's. */
  #armature { text-align: left; }
  .arm-grid { display: grid; grid-template-columns: minmax(0, 1.25fr) minmax(0, 1fr); gap: 2.75rem; align-items: center; }
  @media (max-width: 900px) { .arm-grid { grid-template-columns: 1fr; gap: 1.75rem; } }

  .arm-body { display: flex; flex-direction: column; align-items: flex-start; gap: 1rem; }
  .arm-head { margin: 0; font-family: var(--font-display); font-weight: 800; font-size: clamp(1.6rem, 3.4vw, 2.4rem); line-height: 1.05; letter-spacing: -0.02em; color: var(--bone); }
  /* The headline and this sentence are one sentence across two sizes, exactly as on the
     landing page, so the gap between them stays tight. */
  .arm-lead { margin: -0.35rem 0 0; font-size: 1.0625rem; line-height: 1.6; color: var(--bone); max-width: 40rem; text-wrap: pretty; }
  .arm-note { margin: 0; font-size: 0.875rem; line-height: 1.6; color: var(--bone-faint); max-width: 40rem; text-wrap: pretty; }
  .arm-ctas { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0.25rem; }

  /* WORK WITH ME: the page's only ask. It had none. */
  .hb-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
    padding: 2.5rem 2.75rem;
    border-radius: 20px;
    background: linear-gradient(120deg, rgba(255, 122, 61, 0.1), rgba(255, 122, 61, 0)), var(--carbon);
    border: 1px solid rgba(255, 122, 61, 0.35);
    border-left: 3px solid var(--ember);
    box-shadow: var(--shadow-lg);
  }
  @media (max-width: 640px) { .hb-card { padding: 1.75rem 1.5rem; } }
  .hb-head { margin: 0; font-family: var(--font-display); font-weight: 800; font-size: clamp(1.6rem, 3.2vw, 2.25rem); line-height: 1.05; color: var(--bone); }
  .hb-line { margin: 0; font-size: 1rem; line-height: 1.65; color: var(--bone-dim); max-width: 54rem; text-wrap: pretty; }
  .hb-ctas { display: flex; align-items: center; gap: 1.5rem; flex-wrap: wrap; margin-top: 0.5rem; }
  .hb-mail { font-family: var(--font-mono); font-size: 0.9375rem; color: var(--bone-dim); text-decoration: none; border-bottom: 1px solid var(--iron-3); }
  .hb-mail:hover { color: var(--ember); border-color: var(--ember); }

  #about { text-align: left; }
</style>
