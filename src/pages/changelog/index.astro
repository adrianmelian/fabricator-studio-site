---
// Public changelog (2026-07-20). Required by knowledge/launch-posture.md, which calls it the
// highest-leverage item on the launch list: "Fast public fixes are what convert a first bug
// into loyalty."
//
// This is the PUBLIC layer, deliberately not a mirror of the toolset repo's CHANGELOG.md.
// That file is an internal engineering log (module names, spec numbers, retired code paths)
// and its pre-v1 entries describe work nobody outside the studio ever saw. The public log
// starts at v1.0.0, the first release anyone could install.
//
// VISUAL PASS: Adrian x Claude Design drop, 2026-07-20 (source: MrMiata inbox/HTML/
// "Changelog.html"). Adopted: the two-column release layout with a sticky version rail
// (version pill, release name, spaced date), group cards with an accent bar and alternating
// plasma/ember tint, and the accent chevron item marker. NOT adopted: the drop's fixed
// 1740px canvas (ported responsive, rail collapses above the content under 60rem) and its
// raw hex palette (uses site tokens). The drop held the copy verbatim; its only additions
// were inline bold on product names, which is emphasis, not rewording, so those were kept.
//
// HOW TO ADD A RELEASE: prepend an entry to RELEASES. Keep each line about what changed for
// the person using it, not the code that moved. Fixed bugs should also come off the
// known-issues page in the same pass.
//
// DATE: v1.0.0's date is the day it actually ships. Confirm before deploying.
import BaseLayout from '../../layouts/BaseLayout.astro';
import NavBar from '../../components/NavBar.astro';
import Footer from '../../components/Footer.astro';
import '../../styles/designed-doc.css';

const TITLE = 'CHANGELOG';
const TRI = ['t-bone', 't-plasma', 't-ember'];
let li = 0;
const titleGlyphs = TITLE.split('').map((ch) => ({ ch, cls: ch === ' ' ? 't-gap' : TRI[li++ % 3] }));

// Armature releases, kept in their OWN array and rendered in their own banded section above
// the toolset's (spec item 6, 2026-08-01). Not folded into RELEASES on purpose: that list is
// the Maya Toolset's version line, and dropping a "2.0.0" on top of "1.1.3" would read as the
// toolset jumping four major versions overnight. Two products, two version lines, one page.
const ARMATURE_RELEASES = [
  // Copy source: the app's own in-app changelog (app/src/changelog.ts). Both of these came
  // from ONE outside reporter (GitHub issues 33 and 34), which is why both credit them by
  // name: a visible "reported by" is the cheapest thing that turns a first bug report into
  // a second one.
  {
    version: '2.1.5',
    date: '2026-08-07',
    dateLabel: '2026 · 08 · 07',
    tag: 'Fix release',
    lede: 'Startup survives a graphics driver that stalls, instead of hanging on a blank window.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Startup no longer hangs when Windows forces the app onto a specific GPU.</b> If the graphics driver stalls, Armature now recovers automatically onto a working graphics path and remembers it. If it ever has to fall back to software rendering, it says so plainly and tells you how to restore full speed. Reported by <b>enqui</b>.',
        ],
      },
    ],
  },
  // Copy source: the app's own in-app changelog (app/src/changelog.ts).
  {
    version: '2.1.4',
    date: '2026-08-07',
    dateLabel: '2026 · 08 · 07',
    tag: 'Fix release',
    lede: 'No dead ends: a stopped download can be resumed, and nothing sits greyed out without a way forward.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>An interrupted AutoSkin download announces itself</b> when the app starts, with a one-click Resume that picks up exactly where it stopped. No more reinstalling to recover. Reported by <b>enqui</b>.',
          '<b>The AutoSkin installer has a permanent home</b>, under Help menu &gt; AutoSkin component. The SkinTokens options are never greyed out anymore either: if the component is missing, clicking one takes you straight to the install.',
        ],
      },
    ],
  },
  // Copy source: the app's own in-app changelog (app/src/changelog.ts).
  {
    version: '2.1.3',
    date: '2026-08-04',
    dateLabel: '2026 · 08 · 04',
    tag: 'Fix release',
    lede: 'Textures survive the trip in. A USD imported alongside its textures folder now arrives with its materials.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>USD textures come through on import</b>. Keep the .usd and its textures folder together, the way Maya writes them, and the materials arrive with the model instead of the mesh landing untextured.',
        ],
      },
    ],
  },
  // Copy source: the app's own in-app changelog (app/src/changelog.ts). This one completes
  // the round trip the site started claiming today: a Maya rig comes back as modules, not
  // as a pile of joints wearing the right names.
  {
    version: '2.1.2',
    date: '2026-08-04',
    dateLabel: '2026 · 08 · 04',
    tag: 'Feature release',
    lede: 'The circle closes. A rig imported from Maya arrives as real modules, so the work you did in Armature survives the trip out and back.',
    groups: [
      {
        label: 'NEW',
        tone: 'plasma',
        items: [
          '<b>Imported rigs arrive as modules, not loose joints</b>. Spine, arms and legs come back as proper rows with rename, mirror, twist steppers, regions and the full range-of-motion tools, rather than as a hierarchy you have to re-teach the app about. Twists regenerate cleanly and the skinning follows them by name.',
          '<b>Rig in Maya, skin in Armature, export</b>. Your module build survives the whole round trip.',
        ],
      },
    ],
  },
  // Copy source: the app's own in-app changelog (app/src/changelog.ts).
  {
    version: '2.1.1',
    date: '2026-08-04',
    dateLabel: '2026 · 08 · 04',
    tag: 'Fix release',
    lede: 'Rig in Maya, skin in Armature. A USD that carries a skeleton but no skinning now imports the skeleton and drops you straight into the binding flow.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Weights are no longer required to bring a rig home</b>. Importing a rigged USD used to expect skin weights alongside the skeleton, so a character that was rigged but not yet skinned had nowhere to land. It now imports the skeleton and opens the binding flow, which is the whole point of bringing it into Armature.',
        ],
      },
    ],
  },
  // Copy source: the app's OWN in-app changelog (app/src/changelog.ts), which its test suite
  // now requires before a version can ship. Same words the What's New card shows after an
  // update, so the page and the app cannot tell a user two different stories.
  {
    version: '2.1.0',
    date: '2026-08-03',
    dateLabel: '2026 · 08 · 03',
    tag: 'Feature release',
    lede: 'The first feature release since launch. Save your own modules and templates into the library, bring rigged characters back in from USD, and tag a chain as dynamic so it simulates in Unreal.',
    groups: [
      {
        label: 'NEW',
        tone: 'plasma',
        items: [
          '<b>Save your work into the library</b>. Right-click a joint or module for "Save as module", or save the whole character as a template. Your cards appear under "Yours" and place exactly like the built-ins.',
          '<b>Rigged USD files open with their skeleton and skinning intact</b>, so a character can come back in from Maya or from your own exports rather than starting again.',
          '<b>Dynamic chains</b>. Check "Dynamic" on a chain module and it simulates in Unreal at rig-build time, colliding with the body. The Cape preset ships first.',
        ],
      },
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Textures land correctly in Maya and Unreal</b>. Exported UVs now follow the USD convention, and materials carry the full set: color, roughness, metallic and normal.',
          'When a USD export cannot embed its rig data, the dialog now says exactly what went wrong instead of failing vaguely.',
          'The app version shows in the title bar, which makes bug reports easier to answer.',
        ],
      },
    ],
  },
  // Copy source: the launch-week triage ledger (MrMiata workspace/2026-07-22_authoring-platform/
  // TRIAGE.md, BUG-002). Same blind spot as BUG-001, named honestly: dev machines all carry
  // the VC++ redistributable, a clean laptop does not.
  {
    version: '2.0.2',
    date: '2026-08-02',
    dateLabel: '2026 · 08 · 02',
    tag: 'Fix release',
    lede: 'The second same-day fix from founding-user machines. USD export failed on computers without Microsoft’s C++ runtime installed, which is most computers that have never had a game or a dev tool on them.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>The USD converter now ships its own Microsoft C++ runtime</b>. Our bundled Python carried one of the two runtime DLLs but relied on the machine for the other, so the converter’s USD library failed to load on any computer without the Visual C++ redistributable. Every development machine has it; a clean laptop does not. The build now bundles both DLLs and refuses to produce an installer without them, so this cannot quietly return.',
          '<b>Confirmed working in the wild, one hour after it shipped</b>: the 2.0.1 port fallback moved the converter off a taken port on the very first user machine that needed it.',
        ],
      },
    ],
  },
  {
    version: '2.0.1',
    date: '2026-08-02',
    dateLabel: '2026 · 08 · 02',
    tag: 'Fix release',
    lede: 'Reported by the first founding users within hours of launch, fixed and shipped the same day. If USD export told you the converter was not running, this release is for you.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>USD export no longer requires the AutoSkin download</b>. The app misread one field of the converter’s health report, so on machines without the optional AI component a healthy converter was reported as "not running" and USD export was blocked. Every machine in our loop had the component installed, which is exactly why the first fresh installs found this before we did. Tests now pin the healthy-without-AutoSkin case so it cannot come back.',
          '<b>Cold starts tell the truth</b>. On a fresh install the converter can take a few seconds to wake. The export window now keeps checking while open, says the converter is starting instead of declaring it dead, and waits longer before giving up.',
          '<b>Port conflicts no longer kill the converter</b>. If another app owns its port, Armature finds a free one before anything starts. Healthy machines see no change.',
          '<b>A Restart button at the export surface</b> respawns the converter on the spot, so relaunching the whole app is never the remedy.',
        ],
      },
    ],
  },
  {
    version: '2.0.0',
    date: '2026-08-01',
    dateLabel: '2026 · 08 · 01',
    tag: 'First release',
    lede: 'Armature is out. A standalone app that takes a character mesh in and hands back a rigged, skinned, engine-ready character, with no Maya licence needed. Free for personal work and for studios under $100k a year.',
    groups: [
      {
        label: 'THE APP',
        tone: 'plasma',
        items: [
          'Load a mesh, fit it to a real-world height, place a skeleton from a template or from modules, generate skin weights, and export one USD carrying mesh, skeleton, weights and textures together.',
          '<b>Two skinning engines, both local.</b> Geodesic Voxel solves in seconds on any machine, with no download and no particular GPU. SkinTokens is the AI engine, and is where the quality lives.',
          '<b>The safe, versioned loop.</b> Every skin lands as a pass you can keep, overwrite or compare side by side, so fixing a joint never costs you a skin you liked.',
          'Joint placement tools one keystroke deep: extrude, snap to the mesh, snap to other joints, and automatic symmetry that names sides as joints cross the centre line.',
          'Range-of-motion exercises for every chain, so you can prove a spine or a tail bends the way you expect before you skin it.',
        ],
      },
      {
        label: 'INTO YOUR ENGINE',
        tone: 'ember',
        items: [
          'The exported USD imports into Unreal and Maya, and the rig blueprint travels inside the file, so a character can come back into Armature later.',
          'Auto-update is on. Armature checks quietly on launch, downloads in the background, and installs when you quit.',
        ],
      },
    ],
  },
];

const RELEASES = [
  // 1.4.0 copy source: the release folder's approved draft, checked against the toolset repo's
  // own CHANGELOG entry. The updater it announces also required a privacy-page disclosure, which
  // ships in the same deploy: a second network call retires the "only call" sentence there.
  {
    version: '1.4.0',
    date: '2026-08-04',
    dateLabel: '2026 · 08 · 04',
    tag: 'Feature release',
    lede: 'The last version you download by hand. The toolset updates itself now, and a model can travel to Armature unskinned.',
    groups: [
      {
        label: 'AUTO UPDATER',
        tone: 'plasma',
        items: [
          '<b>The toolset updates itself</b>. Maya checks quietly at startup and shows one small note when a new version exists. One click in Settings downloads the release, verifies its published fingerprint before touching anything, backs up your current install, and updates. Your project configs and any installed pack survive untouched, a failed update restores itself, and the restart prompt is never forced.',
          '<b>Off if you want it off</b>. Settings gains an Updates section with the startup toggle and a Check Now button. Studio machines linked to a shared or development install are detected and left alone. What the check sends, which is nothing about you, is written out on the <a href="/privacy/">privacy page</a>.',
        ],
      },
      {
        label: 'USD EXPORT',
        tone: 'ember',
        items: [
          '<b>Skinning is optional</b>. A model and skeleton with no skin weights now export to USD cleanly, so you can build and joint a character in Maya and do the skinning in Armature. The FBX path still expects a bound mesh, and skinned USD exports behave exactly as before.',
        ],
      },
    ],
  },
  // 1.3.0 copy source: the toolset repo's own CHANGELOG.md entry for v1.3.0, written at the
  // cut. Its [Unreleased] section (toolset self-update) is deliberately NOT here: it is built
  // and not downloadable, which is the exact shipped-versus-demonstrated line R25 was written
  // to hold. It lands the day a release carries it.
  {
    version: '1.3.0',
    date: '2026-08-03',
    dateLabel: '2026 · 08 · 03',
    tag: 'Feature release',
    lede: 'The road out of Maya. The Rig Exporter can now write USD, so a rig you built in Maya opens in Armature as named modules and imports into Unreal from the same file.',
    groups: [
      {
        label: 'USD EXPORT',
        tone: 'plasma',
        items: [
          '<b>One file for Armature and Unreal</b>. Export Options gains a Format choice: FBX, still the default, or USD. USD writes a single delivery file per skeletal entry, carrying the mesh, the skeleton, normalized weights and materials with their textures beside the file.',
          '<b>A Fabricator rig travels as modules, not loose joints</b>. The export embeds your module layout and blueprint, so Armature ingests the character with its arms, legs and spine named and understood. Unreal reads the same file with its own USD importer.',
          '<b>The export checks its own output before shipping it</b>. Root frames, weights, joint names and the embedded data are read back from the written bytes; a file that does not verify is deleted and the reason named, rather than leaving a plausible-looking corpse on disk.',
        ],
      },
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Skeletal export is faster and no longer crashes at exit</b>. The throwaway export process stopped loading the toolbar and Qt it never needed, which takes roughly ten seconds off every export and fixes a crash that could fail an export after it had already succeeded.',
        ],
      },
    ],
  },
  // 1.2.0 copy source: MrMiata workspace/2026-07-31_fs-1.2.0-release/CHANGELOG-DRAFT.md,
  // corrected by the Tools lane against the shipped zip (R26-ack, 2026-08-01): real publish
  // date, and the Advanced item names ribbon spine/arms/legs only (necks build as FK today).
  {
    version: '1.2.0',
    date: '2026-08-01',
    dateLabel: '2026 · 08 · 01',
    tag: 'Feature release',
    lede: 'The bridge release. A character you rig in Armature now walks into Maya and comes out the other side with a working control rig.',
    groups: [
      {
        label: 'ARMATURE IMPORT',
        tone: 'plasma',
        items: [
          '<b>Turn an Armature export into a Fabricator rig</b>. The new Armature button on the toolbar reads the USD you exported from Armature and stands the character up in your scene. The mesh, the skeleton and the skin weights come straight from the file; nothing is rebuilt and no weights are transferred. The import writes a Fabricator blueprint from the rig Armature embedded, assigns components, applies your authored aimers, and leaves Build Rig as the next click.',
          '<b>Simple or Advanced, one choice</b>. Simple assigns the free set: FK spine and neck, IK arms and legs. Advanced assigns the ribbon spine (headed by the pelvis) and ribbon arms and legs when the Advanced Ribbon Modules pack is installed, and tells you why when it is not.',
          '<b>Imported rigs are checked by posing, not by looking</b>. A joint frame rolled off its bone can read perfectly at bind and still twist the moment anything rotates. After import, drivers are posed and the motion is measured, so a rolled frame or skin that stopped deforming is reported before you build on it.',
          '<b>Refusals name their reason</b>. A USD from another tool, a scene that is not empty, an export from a newer Armature: each one stops before touching anything and says what to do instead.',
        ],
      },
      {
        label: 'POLE VECTORS',
        tone: 'ember',
        items: [
          '<b>Leg pole vectors build in front of the knee now</b>. On many characters the left leg\'s PV control landed in front of the right leg and the right leg\'s in front of the left. A near-straight leg was letting millimetres of bind-pose noise pick the direction; placement now follows the knee joint\'s own forward axis, which also respects Y-forward and Z-forward joint conventions and deliberately angled creature knees. Simple IK, IK Leg, both ribbon limbs and the quad leg all share the fix.',
          '<b>A pole vector marker in edit mode</b>. Every IK limb spawns a locator cross where its PV control will build. Leave it and placement is automatic; drag it and the control builds exactly there, including behind the knee for a digitigrade leg. The auto pole vector space and the solver follow the marker too.',
        ],
      },
    ],
  },
  {
    version: '1.1.3',
    date: '2026-07-28',
    dateLabel: '2026 · 07 · 28',
    tag: 'Patch',
    lede: 'Reported in the morning, shipped the same day. If you restyle your controls, take this one.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Changing a control’s shape no longer breaks the IK/FK switch</b>. Swapping the shape on an FK control, an IK hand or a pole vector in the Ctrl Editor left that control on screen in the mode that should have hidden it. The switch hides controls by their shape, so replacing the shape dropped the connection doing the hiding. Swapping now carries that across, along with the control’s colour. Affects every IK limb, including the Advanced Ribbon Pack’s arm and leg.',
          '<b>Rigs already showing this get repaired</b>. The pre-build checks now spot the dropped connection and offer a one-click Reconnect visibility. Building the rig fixes it as well, and your control shape edits survive a rebuild, so the shapes you made stay yours either way.',
        ],
      },
    ],
  },
  {
    version: '1.1.2',
    date: '2026-07-25',
    dateLabel: '2026 · 07 · 25',
    tag: 'Patch',
    lede: 'Second fix of the day, also from the field. If you own the Advanced Ribbon Pack, take this one.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Updating Fabricator no longer removes the Advanced Ribbon Pack</b>. The updater rebuilds the install folder from the new version, and that step was silently deleting the pack’s ribbon modules and Advanced templates. It now carries add-on pack files across the update, including packs installed before this fix.',
          '<b>Ribbon Pack 1.0.1</b> records what it installed, which is what makes the carry-over exact. If your pack already went missing after the last update: re-run RibbonPack_Install.py once and restart Maya. From here on, updates keep it.',
        ],
      },
    ],
  },
  {
    version: '1.1.1',
    date: '2026-07-25',
    dateLabel: '2026 · 07 · 25',
    tag: 'Patch',
    lede: 'One fix, shipped the day after it was reported.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>Templates load onto an existing skeleton again</b>. Dropping a template into a scene that already held its skeleton failed with "Armature: no root joint". The load now adopts the joints already in your scene, creates only what is missing, and stands the Armature up as usual. Partial matches (just the root, or part of the hierarchy) work too.',
        ],
      },
    ],
  },
  {
    version: '1.1.0',
    date: '2026-07-22',
    dateLabel: '2026 · 07 · 22',
    tag: 'Feature release',
    lede: 'Fabricator opens into an empty window, and its two ways in are the two least visible controls in it. This release is the fix for that first minute, plus four things the first outside users asked for.',
    groups: [
      {
        label: 'GETTING STARTED',
        tone: 'plasma',
        items: [
          '<b>A guided tour of Fabricator</b>, offered the first time you open it and available any time after from File > Take the Tour. Eight stops in two parts: one names the four regions of the window, the other builds a rig with you.',
          '<b>The hands-on stops wait for you</b>. They advance when the template actually loads, when the build actually finishes, and when you actually drop back to Edit Mode, and each one says what it is waiting for. Nothing to accidentally click past.',
          '<b>The tour reads your scene</b> rather than assuming an empty one. If you already have a rig loaded it starts where you are, and if that rig is built it offers to drop you back to Edit Mode first.',
          '<b>Armature Tools is now a labelled group</b>, and the Components, Rig Outliner and Properties headers pick up the same accent, so the window reads as four named regions instead of loose chrome.',
        ],
      },
      {
        label: 'ARMATURE',
        tone: 'ember',
        items: [
          '<b>Solo handles</b>. Drag one joint without taking its whole subtree with it. The main control still moves the whole branch. Show, hide and reset live under Skeleton.',
          '<b>Straighten Mid Joint</b>. The classic knee or elbow fix, corrected on one axis only so the deliberate forward bend survives. Most straighten tools snap the joint onto the line between its neighbours, which eats the bend. This one picks the error axis for you and says which it chose.',
          '<b>Aim All at Child</b>. Points every aimer down its chain in one pass. This is the repair for an imported skeleton whose aimers all sit on Local with nothing aimed at anything, which used to mean setting every target by hand in the Channel Box.',
        ],
      },
      {
        label: 'EXPORT',
        tone: 'plasma',
        items: [
          '<b>Right-click a row to export just that entry or clip</b>, in either exporter. No checkboxes, no multi-select.',
        ],
      },
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          'The first launch welcome tour ended early instead of skipping a stop when the button it wanted to point at was not on your toolbar.',
          'Fabricator could open a second window on top of a live one if a rebuild raced a window that had not finished closing.',
        ],
      },
    ],
  },
  {
    version: '1.0.1',
    date: '2026-07-21',
    dateLabel: '2026 · 07 · 21',
    tag: 'Patch',
    lede: 'First patch, straight from the first outside installs. Thank you to everyone who ran it early and told us what broke.',
    groups: [
      {
        label: 'FIXES',
        tone: 'ember',
        items: [
          '<b>AutoSkin installs on a fresh machine</b>. The engine installer used to require uv and git to already be present and failed otherwise; it now downloads pinned, verified copies itself, so a clean artist machine installs with nothing pre-set-up.',
          '<b>AutoSkin uninstall cleans up fully on Windows</b>. It used to leave its model checkpoints (about 1.6 GB) behind; now it removes everything it created.',
        ],
      },
      {
        label: 'IMPROVEMENTS',
        tone: 'plasma',
        items: [
          '<b>Project bindings live in Project Setup now</b>. The per-project folder bindings and the shared configs pointer moved out of Settings and next to the project list they belong with. Settings keeps only the truly per-machine options.',
        ],
      },
    ],
  },
  {
    version: '1.0.0',
    date: '2026-07-20',
    dateLabel: '2026 · 07 · 20',
    tag: 'First release',
    lede: 'The first public release of the FabricatorStudio Maya Toolset: a Maya rigging, skinning, animation, and export pipeline, <b>free for solo developers, indies, and small teams</b>.',
    groups: [
      {
        label: 'RIGGING',
        tone: 'plasma',
        items: [
          '<b>Rig Fabricator</b>, a network driven modular rigging system. Drag components onto your skeleton, build the whole rig in one pass, and rebuild it as often as you like without losing work.',
          'The <b>Armature</b> skeleton builder, with live aimers you can see and adjust while you work.',
          'Save a finished rig as a <b>blueprint</b> and rebuild it on a differently proportioned character.',
          '<b>Limb units</b> group a chain and its twists and fingers together, and derive themselves from the scene rather than being hand maintained.',
          'Selectable mirror conventions: <b>Mirrored Behavior</b> for Unreal, or <b>Mirrored Orientation</b> for studios that keep the same axis down the bone on both sides.',
          'Rename anything. Rig state lives in the scene as connections, not name strings.',
        ],
      },
      {
        label: 'SKINNING',
        tone: 'ember',
        items: [
          '<b>AutoSkin</b>, one button machine learning skinning that runs on your own GPU. It predicts the weights and writes them onto a fresh skinCluster in about two minutes, and it can generate the skeleton from the mesh as well.',
          'Influences you locked keep the weights you painted by hand.',
          '<b>Skin IO</b> and <b>Skeleton IO</b> for moving weights and skeletons between scenes and characters.',
        ],
      },
      {
        label: 'ANIMATION',
        tone: 'plasma',
        items: [
          '<b>Animation Studio</b>, a pose and clip library that carries across characters, with selection sets to mask what an imported pose touches.',
          'Animator friendly controls out of the box: IK and FK matching, space switching, smart pole vectors, and a marking menu on every control.',
        ],
      },
      {
        label: 'EXPORT',
        tone: 'ember',
        items: [
          'Static, skeletal, and animation <b>FBX exporters</b>, with the rig layer understood so you can export an armature, built modules, or bare joints.',
          'Structural checks run before a build and surface problems in <b>Build Issues</b>.',
          '<b>Project Setup</b> holds per project paths, FBX presets, and session settings, from engine templates.',
        ],
      },
      {
        label: 'IN MAYA',
        tone: 'plasma',
        items: [
          '<b>Bridge</b>, a compact toolbar that puts the whole toolset one click away.',
          '<b>Reggie</b>, an AI Technical Artist you connect to your own AI key. Read only, enforced in code, and able to file a bug report for you with the details filled in.',
          'Hover cards on the toolbar and palette, pulled from the same docs published on this site.',
          'A one drag installer that wires the shelves, menu, and hotkeys, and a four step welcome tour on first launch.',
        ],
      },
    ],
  },
];
---
<BaseLayout
  title="Changelog | FabricatorStudio"
  description="Every FabricatorStudio Maya Toolset release, what changed in it, and when it shipped."
>
  <NavBar />

  <article class="dd cl">
    <header class="dd-header">
      <div class="dd-eyebrow">MAYA TOOLSET &middot; CHANGELOG</div>
      <h1 class="dd-title" aria-label={TITLE}>
        {titleGlyphs.map((g) =>
          g.cls === 't-gap' ? <span class="t-gap"></span> : <span aria-hidden="true" class={g.cls}>{g.ch}</span>
        )}
      </h1>
      {/* Scoped to the toolset (Adrian, 2026-07-30). Armature ships its own version line
          (2.0.0) and its releases are announced in the Discord changelog channel; if they
          ever want a page here, it is a SECOND changelog, not more rows in this one.

          The entries below say "Armature" because that is what shipped, including quoted
          error strings and UI labels. Rewriting them would make the history false, so the
          note under this line disambiguates instead. */}
      <p class="dd-summary">What shipped, and when. Bugs we already know about live on the <a href="/known-issues/">known issues page</a>.</p>
      <p class="dd-summary cl-note">Two products, two version lines. Armature is a standalone app at 2.1.3; the Maya Toolset is at 1.4.0, and its entries before August 2026 use <b>Armature</b> for the Maya skeleton stage, which is what it was called at the time. That stage is now <a href="/docs/tools/armature/">Skeleton Building</a>.</p>
    </header>

    <div class="cl-band" aria-label="Armature releases">
      <p class="cl-band__label">ARMATURE &middot; THE STANDALONE APP</p>
    </div>

    {ARMATURE_RELEASES.map((rel) => (
      <section class="cl-rel" aria-labelledby={`cl-armature-${rel.version}`}>
        <div class="cl-rail">
          <div class="cl-ver">
            <span class="cl-ver__dot" aria-hidden="true"></span>
            <h2 id={`cl-armature-${rel.version}`} class="cl-ver__num">v{rel.version}</h2>
          </div>
          <p class="cl-rail__tag">{rel.tag}</p>
          <time class="cl-rail__date" datetime={rel.date}>{rel.dateLabel}</time>
        </div>
        <div class="cl-body">
          <p class="cl-lede" set:html={rel.lede} />
          {rel.groups.map((g) => (
            <div class={`cl-group cl-group--${g.tone}`}>
              <div class="cl-group__head">
                <span class={`cl-group__bar cl-group__bar--${g.tone}`} aria-hidden="true"></span>
                <h3 class={`cl-group__label cl-group__label--${g.tone}`}>{g.label}</h3>
              </div>
              <ul class="cl-list">
                {g.items.map((item) => <li set:html={item} />)}
              </ul>
            </div>
          ))}
        </div>
      </section>
    ))}

    <div class="cl-band" aria-label="Maya Toolset releases">
      <p class="cl-band__label">MAYA TOOLSET</p>
    </div>

    {RELEASES.map((rel) => (
      <section class="cl-rel" aria-labelledby={`cl-${rel.version}`}>
        <div class="cl-rail">
          <div class="cl-ver">
            <span class="cl-ver__dot" aria-hidden="true"></span>
            <h2 id={`cl-${rel.version}`} class="cl-ver__num">v{rel.version}</h2>
          </div>
          <p class="cl-rail__tag">{rel.tag}</p>
          <time class="cl-rail__date" datetime={rel.date}>{rel.dateLabel}</time>
        </div>

        <div class="cl-body">
          <p class="cl-lede" set:html={rel.lede} />

          {rel.groups.map((g) => (
            <div class={`cl-group cl-group--${g.tone}`}>
              <div class="cl-group__head">
                <span class={`cl-group__bar cl-group__bar--${g.tone}`} aria-hidden="true"></span>
                <h3 class={`cl-group__label cl-group__label--${g.tone}`}>{g.label}</h3>
              </div>
              <ul class="cl-list">
                {g.items.map((item) => (
                  <li class="cl-item">
                    <span class={`cl-item__mark cl-item__mark--${g.tone}`} aria-hidden="true">&#9656;</span>
                    <span set:html={item} />
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </section>
    ))}
  </article>

  <Footer />
</BaseLayout>

<style>
  /* Centered page header, matching the licensing page's .lc .dd-header rule. */
  .cl .dd-header { align-items: center; text-align: center; }

  /* The naming note: quieter than the summary, since it is an aside about a rename rather
     than part of the page's promise. */
  .cl-note { font-size: 0.875rem; color: var(--bone-faint); }
  .cl-note b { color: var(--bone-dim); }
  .cl-note a { color: var(--plasma); }

  /* Product bands. Two version lines share this page, so each list gets a labelled rule and a
     reader can never mistake Armature 2.0.0 for the toolset jumping four majors. */
  .cl-band { margin: 0 0 1.5rem; padding-top: 0.5rem; border-top: 1px solid var(--iron-2); }
  .cl-band__label {
    margin: 0.75rem 0 0;
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 0.75rem;
    letter-spacing: 0.18em;
    color: var(--ember);
  }
  .cl-note a { color: var(--plasma); }

  .cl-rel { display: grid; grid-template-columns: 16rem 1fr; gap: 3rem; align-items: start; margin: 0 0 4rem; }

  /* Sticky version rail (design drop) */
  .cl-rail { position: sticky; top: 2.5rem; display: flex; flex-direction: column; gap: 1rem; }
  .cl-ver {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    width: fit-content;
    padding: 0.55rem 1.15rem;
    border-radius: var(--radius);
    background: rgba(124, 255, 178, 0.12);
    border: 1px solid rgba(124, 255, 178, 0.45);
  }
  .cl-ver__dot {
    width: 0.65rem;
    height: 0.65rem;
    border-radius: 50%;
    background: var(--plasma);
    box-shadow: 0 0 10px rgba(124, 255, 178, 0.7);
  }
  .cl-ver__num { font-family: var(--font-mono); font-size: 1.35rem; font-weight: 800; color: var(--plasma); margin: 0; }
  .cl-rail__tag { font-family: var(--font-mono); font-size: 1.5rem; font-weight: 800; color: var(--bone); margin: 0; letter-spacing: -0.01em; }
  .cl-rail__date { font-family: var(--font-mono); font-size: 0.85rem; font-weight: 500; letter-spacing: 0.14em; color: var(--bone-faint); }

  .cl-body { min-width: 0; }
  .cl-lede { color: var(--bone-dim); line-height: 1.6; font-size: 1.05rem; margin: 0 0 1.75rem; text-wrap: pretty; }
  .cl-lede :global(b) { color: var(--bone); }

  /* Drop used a #1C2126 -> #171C21 vertical fade. That second stop is off-palette and the
     G6 gate rejects it; the shift was ~3% and invisible, so this is flat --iron instead. */
  .cl-group {
    background: var(--iron);
    border: 1px solid rgba(124, 255, 178, 0.1);
    border-radius: 18px;
    padding: 1.85rem 2rem;
    margin: 0 0 1.15rem;
  }
  .cl-group--ember { border-color: rgba(255, 122, 61, 0.1); }
  .cl-group__head { display: flex; align-items: center; gap: 0.85rem; margin: 0 0 1.1rem; }
  .cl-group__bar { width: 0.5rem; height: 1.6rem; border-radius: 3px; flex: none; }
  .cl-group__bar--plasma { background: var(--plasma); }
  .cl-group__bar--ember { background: var(--ember); }
  .cl-group__label { font-family: var(--font-mono); font-size: 1.15rem; font-weight: 800; letter-spacing: 0.06em; margin: 0; }
  .cl-group__label--plasma { color: var(--plasma); }
  .cl-group__label--ember { color: var(--ember); }

  .cl-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.85rem; }
  .cl-item { display: flex; gap: 0.9rem; color: var(--bone-dim); line-height: 1.55; font-size: 0.95rem; text-wrap: pretty; }
  .cl-item :global(b) { color: var(--bone); }
  .cl-item__mark { flex: none; }
  .cl-item__mark--plasma { color: var(--plasma); }
  .cl-item__mark--ember { color: var(--ember); }

  @media (max-width: 60rem) {
    .cl-rel { grid-template-columns: 1fr; gap: 1.5rem; }
    .cl-rail { position: static; flex-direction: row; align-items: baseline; flex-wrap: wrap; gap: 0.9rem; }
    .cl-rail__date { margin-left: auto; }
  }
  @media (max-width: 40rem) {
    .cl-group { padding: 1.35rem 1.25rem; }
    .cl-rail__tag { font-size: 1.2rem; }
  }
</style>
